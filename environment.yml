name: starrotator-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - astropy
  - matplotlib
  - numpy
  - requests
  - pytest
  - pytest-cov
  - pip
  - pip:
      - pysme-astro